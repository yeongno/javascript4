"use strict";
const aNumber = maybe;
if (maybe === true) {
    const aBoolean = maybe;
    const aString = maybe;
}
if (typeof maybe === 'string') {
    const aString = maybe;
    const aBoolean = maybe;
}
//runtime에 type이 한정됨을 알수 있다.
